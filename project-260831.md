# 🚀 AffiliWrite AI 프로젝트 개발 로그 (260831)

이 파일은 **2026년 8월 31일** 진행된 **"황금 키워드 발굴기 (기회지수 매트릭스) 신규 탭 화면 및 기회지수 분석 엔진 구축"**에 대한 기술 구현 명세와 작업 이력을 남기는 공식 개발 로그입니다.

---

## 📌 주요 변경 배경

블로그 검색 마케팅(블랙키위, 웨어이즈포스트, 판다랭크 등)에서 핵심 지표로 사용되는 **기회지수(월간 총 검색량 ÷ 블로그 누적 문서수)**를 기반으로, **검색 수요는 높으나 경쟁 문서가 적어 글을 작성했을 때 상위 노출 확률이 극대화되는 블루오션 키워드**를 자동으로 발굴하고 원클릭으로 원고 작성을 연동할 수 있는 독립 전용 탭 화면을 신설하였습니다.

---

## 📂 세부 구현 내용

### 1. 🎯 황금 키워드 기회지수 분석 엔진 (`src/services/keywordAnalyzer.js`)
- **기회지수 산출 및 등급 분류 유틸리티**:
  - $\text{기회지수} = \frac{\text{총 검색량}}{\text{블로그 문서 수}}$
  - **황금 등급 체계**:
    - 👑 **초특급 황금** (기회지수 $\ge 500$) : 골드 하이라이트 배지
    - 🔥 **우수 기회** ($100 \le \text{기회지수} < 500$) : 로즈 배지
    - ⚡ **양호 기회** ($30 \le \text{기회지수} < 100$) : 시안 배지
    - 📊 **경쟁 치열** ($\text{기회지수} < 30$) : 뮤트 배지
- **3가지 키워드 수집/발굴 메서드**:
  1. `fetchAiTrendingOpportunityKeywords(category)`: 실시간 핫이슈 및 트렌드 황금 키워드 자동 수집
  2. `expandSeedKeywordAnalysis(seedKeyword)`: 시드 키워드 입력 시 12~15개 연관 롱테일 키워드 및 기회지수 분석
  3. `analyzeCustomKeywordList(rawText)`: 줄바꿈/콤마로 여러 키워드를 붙여넣어 일괄 계산

### 2. 🖥️ 황금 키워드 대시보드 피드 UI (`src/components/KeywordOpportunityFeed.jsx`)
- **스크린샷과 100% 동일한 데이터 테이블 렌더링**:
  - 컬럼: `#` | `키워드` | `총 검색량` | `블로그 문서` | `기회지수` | `황금 등급` | `원고 작성`
  - 네이버 블로그 검색 결과 새창 링크 (`ExternalLink`) 제공
  - 단일 키워드 클릭 시 클립보드 즉시 복사
- **3대 분석 모드 탭 바**:
  - `🔥 실시간 핫이슈 & 트렌드 발굴` (카테고리별 원클릭 필터)
  - `🔍 시드 키워드 연관 롱테일 확장` (검색 인풋 바)
  - `📑 키워드 직접 대량 분석` (대량 텍스트 영역)
- **KPI 지표 요약 카드**:
  - 총 발굴 키워드 수, 👑 초특급 황금 개수, 🔥 우수 기회 개수, 평균 기회지수
- **실시간 필터 & 정렬 & 내보내기**:
  - 컬럼별 정렬(기회지수, 검색량, 문서수, 키워드명)
  - 최소 검색량 필터, 최대 블로그 문서수 필터
  - `📥 엑셀 CSV 내보내기` 및 `📋 키워드 전체 복사`
- **원클릭 원고 작성 연동**:
  - `[✍️ 원고 작성]` 클릭 시, 해당 키워드와 검색 지표가 `[원고 생성기]` 탭으로 즉시 전달되어 원고 자동 작성 준비 완료

### 3. 🌐 메인 앱 라우팅 및 탭 통합 (`src/App.jsx`)
- 상단 네비게이션 헤더에 3번째 탭 **`[황금 키워드]`** 추가 (기존 `[원고 생성기]`, `[트렌드 피드]` 100% 보존).
- `handleSelectKeyword` 핸들러를 통해 선택된 키워드를 `setPrefilledTrend`에 주입하고 `setCurrentTab('generator')`로 부드럽게 화면 전환.

### 4. 🎨 스타일 및 테마 지원 (`src/index.css`)
- `.trend-table-row:hover` 호버 인터랙션 추가 (Morning / Evening 테마 완벽 대응).

---

## 🛠️ 검증 및 빌드 확인
- **Stage 1 (Build Integrity)**: `npm run build` 실행하여 Vite 클라이언트 프로덕션 빌드 정상 통과 (`dist/assets/index-C8ZQHYJ4.js`, `✓ built in 671ms`) 확인.
- **Stage 2 (Runtime & Scope Verification)**: 컴포넌트 변수, 미선언 식별자(0건), 누락된 import(0건) 정적 무결성 검증 완료.
- **Stage 3 (State & Data Flow Integrity)**: 탭 전환, 키워드 검색, 복사, CSV 다운로드, 원고 생성기 딥링크 연동 정상 확인.
- **Stage 4 (No-Live-API-Call Rule)**: 테스트 과정에서 불필요한 라이브 토큰 소모 없이 무결성 검증 통과.
